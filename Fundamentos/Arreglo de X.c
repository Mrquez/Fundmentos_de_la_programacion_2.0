#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) 
{
   int z=0, y=0, x=0 ;
   printf(" Ingrese cuantos numeros escribirá " );
   scanf("%d",& x);
      printf(" Ingrese los numeros que quiera guardar separados por un enter \n" );
     int arreglo[x];
   for(int i=0; i<x; i++){
       scanf("%d",& z);
       arreglo[i];
   
        }
return 0;

}