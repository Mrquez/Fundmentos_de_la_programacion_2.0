#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {

    int edad;
    long bano;
    float peso;
    double hierro;
scanf("%d",&edad);
scanf("%ld",&bano);
scanf("%f",&peso);
scanf("%lf",&hierro);
    return (0);
}