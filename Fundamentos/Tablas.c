#include <stdio.h>
#include<stdlib.h>
#include <math.h>
int main(int argc, char**argv)
{
   int n=1, res=0;
   printf("las tablas de multiplicar del uno al diez son:  \n" );

   

  
   while(n<=10){
       res=n*1;
       printf(", %d",res);
       n++;
   }
   n=1;
    printf("\n");
    
    while(n<=10){
       res=n*2;
       printf(", %d",res);
       n++;
   }
    n=1;
    printf("\n");
       while(n<=10){
       res=n*3;
       printf(", %d",res);
       n++;
   }
    n=1;
    printf("\n");
    
    while(n<=10){
       res=n*4;
       printf(", %d",res);
       n++;
   }
    n=1;
    printf("\n");
    
       while(n<=10){
       res=n*5;
       printf(", %d",res);
       n++;
   }
    n=1;
    printf("\n");
    
    while(n<=10){
       res=n*6;
       printf(", %d",res);
       n++;
   } 
    n=1;
    printf("\n");
    
       while(n<=10){
       res=n*7;
       printf(", %d",res);
       n++;
   }
    n=1;
    printf("\n");
    
    while(n<=10){
       res=n*8;
       printf(", %d",res);
       n++;
   }
    n=1;
    printf("\n");
    
       while(n<=10){
       res=n*9;
       printf(", %d",res);
       n++;
   }
    n=1;
    printf("\n");
    
    while(n<=10){
       res=n*10;
       printf(", %d",res);
       n++;
   }   
        return 0;
}